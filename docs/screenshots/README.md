# Screenshots

Drop image files into this folder so the README renders them. Expected:

| Filename | Shows |
| --- | --- |
| `settings-page.png` | The Links Per Page settings screen. |
| `admin-link-table.png` | The YOURLS admin link table with the configured row count. |
